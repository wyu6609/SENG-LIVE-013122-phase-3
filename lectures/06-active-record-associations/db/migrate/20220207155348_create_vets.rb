class CreateVets < ActiveRecord::Migration[6.1]
  # Activity 1 => Building DB Migrations

  # Write necessary code to create 'vets' table with following
  # columns: vet_name (string), speciality (string)
  
  # Run "rake db:migrate" and verify via "schema.rb" / sqlite3
  
  # BONUS => Generate additional "created_at" and "updated_at" columns using
  # one line of code
  
  def change
  end
end
